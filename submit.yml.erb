batch_connect:
    template: "basic"
    queue_name: "submission"
    script_file: "/srv/scratch/sbf-pipelines/proteinfold/bin/start-alphafold2.sh"

script:
    native:
        - "-A kod_proteinfold"
        - "-l select=1:ngpus=1:ncpus=8:mem=125gb"
        - "-l walltime=12:00:00"
    email:
        - "<%= ENV['USER'] %>@ad.unsw.edu.au"
    email_on_terminated: "<%= email_on_terminated %>"
